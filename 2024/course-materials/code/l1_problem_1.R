# Fix the code on lines 3-6

calculate_mean <- function() {
    mean <- sum(x) / len(x)
    return()
}}

# This line should run once the code works. What is your answer?
round(calculate_mean(runif(1E6)),2)
